name: MDF docs regeneration

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: write

jobs:
  update:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Update MDF generated surfaces
        shell: python
        run: |
          from pathlib import Path

          readme = Path("Hawkynt.FileFormats.FileSystems/README.md")
          text = readme.read_text(encoding="utf-8")
          old = "| [MDF/MDS](https://en.wikipedia.org/wiki/Alcohol_120%25) | `Mdf` | R/W | — | — | — | — | — | ✅ | own reader + struct-parity tests | Sector-image editor | [Alcohol 120%](https://en.wikipedia.org/wiki/Alcohol_120%25) |"
          new = "| [MDF/MDS](https://en.wikipedia.org/wiki/Alcohol_120%25) | `Mdf` | R/W | ✅ | ✅ rebuild | ✅ | — | — | ✅ | own reader + struct-parity tests | Fixed-track ISO 9660 editor; raw-sector EDC/ECC regenerated | [Alcohol 120%](https://en.wikipedia.org/wiki/Alcohol_120%25) |"
          if old not in text:
              raise SystemExit("expected MDF support-matrix row not found")
          readme.write_text(text.replace(old, new, 1), encoding="utf-8")

          roundtrip = Path("Compression.Tests/Operations/FilesystemWriteRoundTripTests.cs")
          tests = roundtrip.read_text(encoding="utf-8")
          old_deferral = '    ["Mdf"] = "MdfInPlaceModifyTests",\n'
          if old_deferral not in tests:
              raise SystemExit("expected MDF unit-addressed deferral not found")
          roundtrip.write_text(tests.replace(old_deferral, "", 1), encoding="utf-8")

      - name: Commit generated surfaces
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- Hawkynt.FileFormats.FileSystems/README.md Compression.Tests/Operations/FilesystemWriteRoundTripTests.cs; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Hawkynt.FileFormats.FileSystems/README.md Compression.Tests/Operations/FilesystemWriteRoundTripTests.cs
          git commit -m "Update MDF capability surfaces"
          git push origin HEAD:${{ github.head_ref }}
