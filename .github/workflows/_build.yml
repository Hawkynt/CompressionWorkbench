name: Build artifacts

# Shared artifact-build block. Called by release.yml (tag push) and nightly.yml
# (workflow_run on CI success). Everything runs on windows-latest because:
#   * SFX stubs must be embedded into Compression.Lib before the final Lib
#     build, and staging is cleaner on one host.
#   * The Linux CLI and the Linux UI publish from a Windows host via --runtime linux-x64,
#     so one runner covers every target.
#
# Inputs:
#   version       full X.Y.Z.BUILD baked into artifact names
#   release-kind  "release" | "nightly" (stub/kind unused here, kept for symmetry)
#   ref           git ref to build (defaults to caller ref)
#
# Produces one artifact `CompressionWorkbench-<version>` containing four zips:
#   * CompressionWorkbench-CLI-win-x64-<version>.zip
#   * CompressionWorkbench-CLI-linux-x64-<version>.zip
#   * CompressionWorkbench-UI-win-x64-<version>.zip
#   * CompressionWorkbench-UI-linux-x64-<version>.zip
on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      release-kind:
        required: true
        type: string
      changelog-flag:
        required: true
        type: string
      ref:
        required: false
        type: string
        default: ""
    outputs:
      artifact-name:
        description: "Name of the uploaded artifact folder (zips of the executables)"
        value: ${{ jobs.build.outputs.artifact-name }}
      nuget-artifact-name:
        description: "Name of the uploaded artifact folder (the five .nupkg files)"
        value: ${{ jobs.build.outputs.nuget-artifact-name }}

# Force JavaScript-based actions to run on Node 24 instead of the deprecated Node 20 ahead of
# the 2026-06-16 hard cutover, until we bump each action to a Node-24-native major version.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  # NativeAOT cannot cross-compile, so each runtime is built on its own runner rather than
  # cross-published from one Windows host the way the rest of this workflow does.
  sfx-stubs-universal:
    name: sfx ${{ matrix.rid }} ${{ matrix.kind }} universal
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        kind: [cli, ui]
        include:
          - { rid: win-x64,    os: windows-latest }
          - { rid: win-arm64,  os: windows-11-arm }
          - { rid: linux-x64,  os: ubuntu-latest }
          - { rid: linux-arm64, os: ubuntu-24.04-arm }
          - { rid: osx-x64,    os: macos-13 }
          - { rid: osx-arm64,  os: macos-latest }
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: preview
      - name: Install the native toolchain AOT links against
        if: startsWith(matrix.os, 'ubuntu')
        run: sudo apt-get update && sudo apt-get install -y clang zlib1g-dev
      - name: Publish
        shell: bash
        run: |
          set -euo pipefail
          proj=$([ "${{ matrix.kind }}" = cli ] && echo Compression.Sfx.Cli || echo Compression.Sfx.Ui)
          ext=$([ "${{ startsWith(matrix.rid, 'win') }}" = true ] && echo .exe || echo "")
          dest="sfx-staging/${{ matrix.rid }}"
          mkdir -p "$dest"
          # ExcludeStubs stops Compression.Lib embedding previously-built stubs into the stub being
          # built, which would grow it without bound.
          dotnet publish "$proj/$proj.csproj" -r ${{ matrix.rid }} -c Release             -p:ExcludeStubs=true -p:SfxTier=Universal -p:Version=${{ inputs.version }} --nologo -o out
          cp "out/sfx-${{ matrix.kind }}$ext" "$dest/sfx-${{ matrix.kind }}-universal$ext"
          ls -l "$dest"
      - uses: actions/upload-artifact@v4
        with:
          name: sfx-stub-${{ matrix.rid }}-${{ matrix.kind }}-universal
          path: sfx-staging
          if-no-files-found: error

  # Carved stubs carry one format each and are five to twelve times smaller than the universal one,
  # so the formats people actually ship as SFX get the small stub. Anything not listed here still
  # works: SfxBuilder falls back to the universal stub.
  sfx-stubs-carved:
    name: sfx ${{ matrix.rid }} ${{ matrix.kind }} ${{ matrix.format }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        kind: [cli, ui]
        format: [Zip, SevenZip, Tar, Rar, Cab, TarGz, TarXz, TarZst]
        include:
          - { rid: win-x64,    os: windows-latest }
          - { rid: win-arm64,  os: windows-11-arm }
          - { rid: linux-x64,  os: ubuntu-latest }
          - { rid: linux-arm64, os: ubuntu-24.04-arm }
          - { rid: osx-x64,    os: macos-13 }
          - { rid: osx-arm64,  os: macos-latest }
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: preview
      - name: Install the native toolchain AOT links against
        if: startsWith(matrix.os, 'ubuntu')
        run: sudo apt-get update && sudo apt-get install -y clang zlib1g-dev
      - name: Publish
        shell: bash
        run: |
          set -euo pipefail
          proj=$([ "${{ matrix.kind }}" = cli ] && echo Compression.Sfx.Cli || echo Compression.Sfx.Ui)
          ext=$([ "${{ startsWith(matrix.rid, 'win') }}" = true ] && echo .exe || echo "")
          suffix=$(echo "${{ matrix.format }}" | tr '[:upper:]' '[:lower:]')
          dest="sfx-staging/${{ matrix.rid }}"
          mkdir -p "$dest"
          dotnet publish "$proj/$proj.csproj" -r ${{ matrix.rid }} -c Release             -p:ExcludeStubs=true -p:SfxTier=Carved -p:SfxFormat=${{ matrix.format }}             -p:Version=${{ inputs.version }} --nologo -o out
          cp "out/sfx-${{ matrix.kind }}$ext" "$dest/sfx-${{ matrix.kind }}-$suffix$ext"
          ls -l "$dest"
      - uses: actions/upload-artifact@v4
        with:
          name: sfx-stub-${{ matrix.rid }}-${{ matrix.kind }}-${{ matrix.format }}
          path: sfx-staging
          if-no-files-found: error

  build:
    needs: [sfx-stubs-universal, sfx-stubs-carved]
    runs-on: windows-latest
    timeout-minutes: 60
    permissions:
      contents: write                           # action-gh-release needs this downstream
    outputs:
      artifact-name: CompressionWorkbench-${{ inputs.version }}
      nuget-artifact-name: nuget-packages-${{ inputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}
          fetch-depth: 0

      # FileFormat.PngCrushAdapters has sibling-conditional ProjectReferences
      # to ..\..\PNGCrushCS\FileFormat.{Png,Apng,Jpeg,...}. Solution build
      # references the sibling for the full image-format surface (test compile,
      # UI publish via PngCrushAdapters).
      - name: Clone PNGCrushCS sibling
        shell: bash
        run: git clone --depth 1 https://github.com/Hawkynt/PNGCrushCS.git "$GITHUB_WORKSPACE/../PNGCrushCS"

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Pre-build PNGCrushCS sibling (Release)
        shell: bash
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE/../PNGCrushCS"
          REQUIRED="Compression.Core FileFormat.Core FileFormat.Bmp FileFormat.Apng \
                    FileFormat.Tiff FileFormat.BigTiff FileFormat.Mng FileFormat.Fli \
                    FileFormat.Dcx FileFormat.Icns FileFormat.Mpo FileFormat.Jpeg \
                    FileFormat.Png"
          # Sibling reorganised most format projects into a FileFormats/ subdir
          # but kept Compression.Core / FileFormat.Core at the root. Probe both
          # locations per project so the script tolerates the layout move.
          # Also tolerate transient sibling-mid-refactor commits where a project
          # is missing entirely — our FileFormat.PngCrushAdapters.csproj has a
          # matching SiblingPresent gate that falls back to an empty assembly.
          locate() {
            local proj="$1"
            if [ -f "$proj/$proj.csproj" ]; then echo "$proj"; return 0; fi
            if [ -f "FileFormats/$proj/$proj.csproj" ]; then echo "FileFormats/$proj"; return 0; fi
            return 1
          }
          MISSING=""
          for proj in $REQUIRED; do
            if ! locate "$proj" >/dev/null; then
              MISSING="$MISSING $proj"
            fi
          done
          if [ -n "$MISSING" ]; then
            echo "::warning::PNGCrushCS sibling incomplete (missing:$MISSING). Skipping pre-build; consumer csproj SiblingPresent gate handles the fallback."
            exit 0
          fi
          for proj in $REQUIRED; do
            dir="$(locate "$proj")"
            echo "::group::Building $proj ($dir)"
            dotnet build "$dir/$proj.csproj" --configuration Release --nologo
            echo "::endgroup::"
            test -f "$dir/bin/Release/net8.0/$proj.dll" || {
              echo "::error::Pre-build did not produce $dir/bin/Release/net8.0/$proj.dll"
              exit 1
            }
          done
          echo "All sibling Release DLLs verified."

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Update CHANGELOG
        shell: bash
        run: node .github/workflows/scripts/update-changelog.mjs ${{ inputs.changelog-flag }}

      - name: Restore
        run: dotnet restore CompressionWorkbench.slnx

      - name: Build solution
        run: dotnet build CompressionWorkbench.slnx --configuration Release --no-restore -p:Version=${{ inputs.version }}

      - name: Collect SFX stubs into Compression.Lib/stubs
        uses: actions/download-artifact@v4
        with:
          pattern: sfx-stub-*
          merge-multiple: true
          path: Compression.Lib/stubs

      - name: Report the stub inventory
        shell: bash
        run: |
          # Every stub embeds into cwb and the shell, so a size regression is worth seeing in the
          # log rather than discovering in a release.
          find Compression.Lib/stubs -type f -exec ls -l {} + | sort -k9
          echo "total: $(du -sh Compression.Lib/stubs | cut -f1)"

      - name: Publish CLI (win-x64)
        run: dotnet publish Compression.CLI/Compression.CLI.csproj -r win-x64 -c Release --self-contained --nologo -p:EnableSingleFileBundle=false -p:Version=${{ inputs.version }}

      - name: Publish CLI (linux-x64)
        run: dotnet publish Compression.CLI/Compression.CLI.csproj -r linux-x64 -c Release --self-contained --nologo -p:EnableSingleFileBundle=false -p:Version=${{ inputs.version }}

      # Compression.NativeUI is the one frontend: a NativeForms shell over the Win32 and GTK
      # backends. It targets plain net10.0, so the Linux binary publishes from this Windows host
      # like the Linux CLI does. PublishSingleFile lives on the command line rather than in the
      # csproj because it needs a RuntimeIdentifier, and the project must stay RID-agnostic for
      # `dotnet run` on either OS.
      - name: Publish UI (win-x64)
        run: dotnet publish Compression.NativeUI/Compression.NativeUI.csproj -r win-x64 -c Release --self-contained --nologo -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:Version=${{ inputs.version }}

      - name: Publish UI (linux-x64)
        run: dotnet publish Compression.NativeUI/Compression.NativeUI.csproj -r linux-x64 -c Release --self-contained --nologo -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:Version=${{ inputs.version }}

      - name: Package CLI (win-x64)
        shell: pwsh
        run: |
          $v = "${{ inputs.version }}"
          $src = "Compression.CLI/bin/Release/net10.0/win-x64/publish/cwb.exe"
          Compress-Archive -Path $src -DestinationPath "CompressionWorkbench-CLI-win-x64-$v.zip"

      - name: Package CLI (linux-x64)
        shell: pwsh
        run: |
          $v = "${{ inputs.version }}"
          $src = "Compression.CLI/bin/Release/net10.0/linux-x64/publish/cwb"
          Compress-Archive -Path $src -DestinationPath "CompressionWorkbench-CLI-linux-x64-$v.zip"

      - name: Package UI (win-x64)
        shell: pwsh
        run: |
          $v = "${{ inputs.version }}"
          $pubDir = "Compression.NativeUI/bin/Release/net10.0/win-x64/publish"
          $shellBuildDir = "Compression.Shell/bin/Release/net10.0-windows"
          $stageDir = "stage-ui"
          New-Item -ItemType Directory -Path $stageDir -Force | Out-Null

          Copy-Item "$pubDir/Compression.NativeUI.exe" $stageDir -Force

          # COM host shim must live beside the exe for Explorer shell-extension registration.
          $comhost = "$shellBuildDir/Compression.Shell.comhost.dll"
          if (Test-Path $comhost) {
            Copy-Item $comhost $stageDir -Force
          }

          Compress-Archive -Path "$stageDir/*" -DestinationPath "CompressionWorkbench-UI-win-x64-$v.zip"

      # Same single-binary shape as the Linux CLI zip. Compress-Archive drops the
      # POSIX execute bit, so the download needs `chmod +x` exactly as cwb does.
      - name: Package UI (linux-x64)
        shell: pwsh
        run: |
          $v = "${{ inputs.version }}"
          $src = "Compression.NativeUI/bin/Release/net10.0/linux-x64/publish/Compression.NativeUI"
          if (!(Test-Path $src)) { throw "Linux NativeUI publish output is missing: $src" }
          Compress-Archive -Path $src -DestinationPath "CompressionWorkbench-UI-linux-x64-$v.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: CompressionWorkbench-${{ inputs.version }}
          path: |
            CompressionWorkbench-CLI-win-x64-${{ inputs.version }}.zip
            CompressionWorkbench-CLI-linux-x64-${{ inputs.version }}.zip
            CompressionWorkbench-UI-win-x64-${{ inputs.version }}.zip
            CompressionWorkbench-UI-linux-x64-${{ inputs.version }}.zip

      # NuGet packages. PackageId for each comes from $(MSBuildProjectName)
      # which equals the .csproj filename — overriding -p:PackageId at the
      # command line propagates globally and triggers NU1201 on referenced
      # projects, so don't pass it here. Pack Hashing first because Core
      # declares it as a NuGet dependency; then Core because the meta-packages
      # declare Core as their NuGet dependency.
      # NB: --no-build is intentionally omitted. The meta-package csprojs use a
      # custom `_AddBundledAssembliesToPackage` target with
      # `DependsOnTargets="ResolveReferences"` to bundle member DLLs into lib/.
      # Under --no-build the SDK refuses to invoke ResolveReferences (NETSDK1085
      # "Build target was invoked"), so we let pack re-validate; outputs from
      # the prior `dotnet build` step are up-to-date so the build phase no-ops.
      - name: Pack Hawkynt.Algorithms.Hashing
        shell: bash
        run: |
          dotnet pack Hawkynt.Algorithms.Hashing/Hawkynt.Algorithms.Hashing.csproj \
            --configuration Release \
            --output dist-nuget \
            -p:Version=${{ inputs.version }} \
            -p:ContinuousIntegrationBuild=true \
            -p:IncludeSymbols=true \
            -p:SymbolPackageFormat=snupkg

      - name: Pack Hawkynt.Compression.Core
        shell: bash
        run: |
          dotnet pack Compression.Core/Hawkynt.Compression.Core.csproj \
            --configuration Release \
            --output dist-nuget \
            -p:Version=${{ inputs.version }} \
            -p:ContinuousIntegrationBuild=true \
            -p:IncludeSymbols=true \
            -p:SymbolPackageFormat=snupkg

      - name: Pack Hawkynt.FileFormats.Audio
        shell: bash
        run: |
          dotnet pack Hawkynt.FileFormats.Audio/Hawkynt.FileFormats.Audio.csproj \
            --configuration Release \
            --output dist-nuget \
            -p:Version=${{ inputs.version }} \
            -p:ContinuousIntegrationBuild=true

      - name: Pack Hawkynt.FileFormats.Archives
        shell: bash
        run: |
          dotnet pack Hawkynt.FileFormats.Archives/Hawkynt.FileFormats.Archives.csproj \
            --configuration Release \
            --output dist-nuget \
            -p:Version=${{ inputs.version }} \
            -p:ContinuousIntegrationBuild=true

      - name: Pack Hawkynt.FileFormats.FileSystems
        shell: bash
        run: |
          dotnet pack Hawkynt.FileFormats.FileSystems/Hawkynt.FileFormats.FileSystems.csproj \
            --configuration Release \
            --output dist-nuget \
            -p:Version=${{ inputs.version }} \
            -p:ContinuousIntegrationBuild=true

      - uses: actions/upload-artifact@v4
        with:
          name: nuget-packages-${{ inputs.version }}
          path: dist-nuget/*