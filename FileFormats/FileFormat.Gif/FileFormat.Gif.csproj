<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>FileFormat.Gif</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Compression.Registry\Compression.Registry.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- GIF byte parsing/LZW lives in PNGCrushCS; this project keeps only the
         CompressionWorkbench-specific composed-frame and block-map adapters. -->
    <PackageReference Include="Hawkynt.FileFormats.Images" Version="1.0.0.131" PrivateAssets="compile" />
  </ItemGroup>

</Project>
