<Window x:Class="Compression.UI.Views.TileContentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tile contents" Width="520" Height="420"
        MinWidth="360" MinHeight="240"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" x:Name="HeaderLbl" FontWeight="SemiBold" FontSize="13"
               Text="Tile @ offset 0x0 (0 B span)" />
    <TextBlock Grid.Row="1" x:Name="SubHeaderLbl" Foreground="DimGray" Margin="0,2,0,6"
               Text="0 entries" />

    <ListBox Grid.Row="2" x:Name="ContentsList"
             FontFamily="Consolas" FontSize="12"
             HorizontalContentAlignment="Stretch"
             ScrollViewer.HorizontalScrollBarVisibility="Auto" />

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
      <Button Content="Close" Width="80" Padding="4" Click="OnClose"
              IsDefault="True" IsCancel="True" />
    </StackPanel>
  </Grid>
</Window>
